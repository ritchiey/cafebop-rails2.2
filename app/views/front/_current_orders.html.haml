-unless @current_orders.empty?
  .span-24.last#my-orders
    %table.my-orders
      %thead
        %tr
          %th(colspan="3") You have Outstanding Orders...
      %tbody
        !=render :partial=>'orders/current_order', :as=>:order, :collection=>@current_orders
