%h3 Nice One!

%p
  Your shop #{@new_shop.name} is now online at
  #{link_to(shop_new_order_path(@new_shop), shop_new_order_path(@new_shop))}.
%p
  We've created some sample menus based on the cuisines you chose but these
  are almost certainly not accurate so the next step is to edit or import
  your menus.

  
  