%table
  %thead
    %tr
      %th Qty
      %th Item
      %th Notes
  %tbody
    -if @orders.empty?
      %td
      %td{:span=>"2"} No Pending Orders
    -else
      = render :partial=>'orders/order', :collection=>@orders

= "Last Updated: #{Time.now}"
                                                   