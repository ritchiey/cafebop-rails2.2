#queue_status
  = render :partial => 'status'

#collection-section
  =render :partial=>'current_items'
= periodically_call_remote :update=>'collection-section', :url=>current_items_item_queue_path(@queue), :frequency=>20, :method=>'GET'
  
