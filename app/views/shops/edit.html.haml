%h3 Edit Shops
   
=render :partial => 'back'
=render :partial=>'form', :object=>@shop
-if @shop.menus.empty?
  No menus yet.
-else
  -@shop.menus.each do |menu|
    %ul
      %li=link_to menu.name, edit_menu_path(menu)
=link_to "Add Menu", new_shop_menu_path(@shop)


  