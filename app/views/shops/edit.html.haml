%h3= "Edit Shop (#{@shop.state} edition)"
   
=render :partial => 'back'
=render :partial=>'form', :object=>@shop

.menus 
  %h4 Menus
  -if @shop.menus.empty?
    No menus yet.
  -else
    %ul= render :partial => @shop.menus
  =link_to "Add Menu", new_shop_menu_path(@shop)

-if @shop.can_have_queues?
  .queues
    %h4 Queues
    -if @shop.item_queues.empty?
      No queues yet.
    -else
      %ul= render :partial => @shop.item_queues
    =link_to "Add Queue", new_shop_item_queue_path(@shop)
  