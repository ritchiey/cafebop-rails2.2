%h2 Shops
%ul
  -@shops.each do |shop|
    %li=link_to shop.name, edit_shop_path(shop)
    =link_to "Place Order", new_shop_order_path(shop)
    -if shop.community?
      =link_to "Claim this shop", shop_claims_path(shop), :method=>:post
=link_to "Add Shop", new_shop_path
